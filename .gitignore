# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo

# Environment variables (secrets)
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# Build output
dist/
frontend/dist/
*.egg-info/
.tmp-*

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Python venv
CSS/
venv/
.venv/
env/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Desktop.ini

# Package lock: pnpm is the package manager, so pnpm-lock.yaml is committed — CI installs with
# `--frozen-lockfile` and fails without it. A lock from any other manager is a mistake, not a choice.
package-lock.json
yarn.lock

# Claude artifacts
.claude/
*/.claude/
