/**
 * Retired. The scoreboard keeps one layout at every width and shrinks its icons instead; see
 * `density.ts`. This file is kept only because the working tree cannot delete it from here. Delete it.
 */
export {};
